<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <!--
      Optional, special permission — granted via a Settings screen, not a
      runtime dialog. Lets OverlayHost keep background tabs attached to a
      real (invisible) window even after MainActivity closes, so they keep
      rendering instead of Chromium treating them as "not visible". Every
      code path that touches this checks Settings.canDrawOverlays() first
      and no-ops if it's not granted — the app is fully functional without
      it, just falls back to the JS-level visibility spoof only.
    -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".MoonliteApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="MoonLite"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true">

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          stopWithTask="false" is the whole point here: without it, Android
          kills this service the instant the user swipes MoonLite out of
          Recents, which would also kill the control server and every open
          tab. With it, the service (and 127.0.0.1:8848) keeps running
          headless until the OS genuinely needs the memory or the user
          force-stops the app.
        -->
        <service
            android:name=".MoonliteService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <!--
          Needed so /upload can hand WebView a real content:// Uri for a
          file this app already wrote to its own cache dir — WebView's
          renderer process can't be given a bare file:// path directly
          (blocked by FileUriExposedException on modern Android), and a
          FileProvider is the standard, permission-scoped way around that.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
